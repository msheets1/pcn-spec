{
  "buildCommand": "cd scripture-web && npm install && npm run build",
  "outputDirectory": "scripture-web/dist",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
